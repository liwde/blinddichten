export const UI_PATH = '../ui/public';
export const SERVER_PORT = 5001;
export const HEARTBEAT_TIMEOUT = 60 * 60 * 1000; // As long as we don't have an app sending a heartbeat, we should be generous
export const HEARTBEAT_INTERVAL = 60 * 1000;
export const CLOSE_EMPTY_GAME_TIMEOUT = 10 * 60 * 1000;
export const DEFAULT_NUM_ROUNDS = 2;
export const DEFAULT_PLAYER_NAME = 'Unbenannter Spieler';
